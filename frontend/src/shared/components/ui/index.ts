// Sprint 13 (T3): this barrel is now a thin re-export of @/components/ui — the canonical
// location for all UI primitives. The duplicate .tsx files in this folder are dead code
// kept temporarily for safety; scheduled for removal in S13-T11 cleanup.
export { Button, Input, Field, Select, Textarea, Modal, Card, Badge, ProgressBar, CircularProgress, ToastContainer, LoadingSpinner, PageLoader, Skeleton, Tabs, TabPanel } from '@/components/ui';
